# MapContext.moveToLocation

Muestra la ubicación fijada en el centro del mapa y restaura el nivel de zoom predeterminado. Utiliza esta API junto con el método `show-location` del componente de mapa.

## Código de ejemplo

```js
this.mapCtx = my.createMapContext('map');
this.mapCtx.moveToLocation();
```